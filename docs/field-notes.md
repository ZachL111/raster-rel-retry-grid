# Field Notes

This note keeps the reliability assumptions visible beside the checks.

The domain cases cover `budget pressure`, `failure width`, `recovery gap`, and `runbook drift`. They sit beside the smaller starter fixture so the project has both a compact scoring check and a domain-flavored review check.

`recovery` is the strongest case at 208 on `runbook drift`. `stress` is the cautious anchor at 125 on `failure width`.

The point is not to make the repository bigger. The point is to make the important judgment testable.
