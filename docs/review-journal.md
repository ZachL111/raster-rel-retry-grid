# Review Journal

I treated `raster-rel-retry-grid` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its reliability focus without claiming live deployment or external usage.

## Cases

- `baseline`: `budget pressure`, score 166, lane `ship`
- `stress`: `failure width`, score 125, lane `watch`
- `edge`: `recovery gap`, score 141, lane `ship`
- `recovery`: `runbook drift`, score 208, lane `ship`
- `stale`: `budget pressure`, score 157, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
